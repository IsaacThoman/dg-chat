server {
  listen 8080;
  server_name _;
  location / {
    proxy_pass http://searxng:8080;
    proxy_set_header Host searxng;
    proxy_set_header Connection "";
    proxy_http_version 1.1;
    proxy_connect_timeout 3s;
    proxy_read_timeout 10s;
    client_max_body_size 64k;
  }
}
